package com.jobready.threading;

public class Sequence {
	
	private int value = 0;
	
	public int getNext() {
		synchronized (this) { // following code is thread safe, one thread at a time
			value++;
			return value;
		}
	}
}
